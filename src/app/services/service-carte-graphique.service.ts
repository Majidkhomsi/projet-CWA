import { Injectable } from '@angular/core';
import { CarteGraphique } from '../models/Carte-Graphique' ;

@Injectable({
  providedIn: 'root'
})
export class ServiceCarteGraphique {
  constructor() { }

  // Méthodes pour gérer les cartes graphiques (obtenir, ajouter, etc.)
}
