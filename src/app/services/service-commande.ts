import { Injectable } from '@angular/core';
import { CommandeComponent } from '../components/commande/commande.component';

@Injectable({
  providedIn: 'root'
})
export class ServiceCommande {
  constructor() { }

  // Méthodes pour gérer les commandes (créer, récupérer, etc.)
}
